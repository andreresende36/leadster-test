name: SonarCloud Analysis

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  sonarcloud:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v1.9.1
    
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: 002fb2e636352463f37db014e4033e5dcea9b348

        with:
          organization: andre-resende
          projectKey: leadster-test
